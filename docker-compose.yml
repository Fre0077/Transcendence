services:
  # 1. RABBITMQ STARTS FIRST
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
      - RABBITMQ_SERVER_ADDITIONAL_ERL_ARGS=-rabbitmq_prometheus
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - transcendence

  # 2. FRONTEND STARTS AFTER RABBITMQ IS HEALTHY
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - RABBITMQ_URL=amqp://trans:strongpassword@rabbitmq:5672
    depends_on:
      rabbitmq:
        condition: service_healthy
    networks:
      - transcendence

# 2.25. MONITORING STUFFS
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitor/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_dat:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
    depends_on:
      rabbitmq:
        condition: service_healthy
      frontend:
        condition: service_started
    networks:
      - transcendence
    restart: always

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3050:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_SECURITY_ADMIN_USER=admin
    volumes:
      - grafana_dat:/var/lib/grafana
      - ./monitor/grafana/datasource.yml:/etc/grafana/provisioning/datasources/datasource.yml
      - ./monitor/grafana/dashboard-provider.yml:/etc/grafana/provisioning/dashboards/provider.yml
      - ./monitor/grafana/master-dashboard.json:/var/lib/grafana/master-dashboard.json
    depends_on:
      - prometheus
    networks:
      - transcendence
    restart: always


  # 2.5 BUTLER che rompe i coglioni
  butler:
    build:
      context: ./backend/gateway/butler
      dockerfile: Dockerfile
    container_name: butler
    depends_on:
      rabbitmq:
        condition: service_healthy
    networks:
      - transcendence
    restart: always

# 3. NGINX HTTPS & Certificate
  nginx:
      image: nginx:stable-alpine
      container_name: nginx
      restart: always
      ports:
        - "8080:80"
        - "4443:4443"
      volumes:
        - ./nginx/conf:/etc/nginx/conf.d:ro
        - ./nginx/ssl:/etc/nginx/ssl:ro
      depends_on:
        - frontend
        - butler
      networks:
        - transcendence

  # 3. BACKEND SERVICES START LAST
  auth:
    build:
      context: ./backend/database/
      dockerfile: auth/Dockerfile
    container_name: auth
    environment:
      - NODE_ENV=production
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
    volumes:
      - auth_db:/app/database
    depends_on:
      rabbitmq:
        condition: service_healthy
      frontend:
        condition: service_started
      butler:
        condition: service_started
    networks:
      - transcendence
    restart: always

  chat:
    build:
      context: ./backend/database/
      dockerfile: chat/Dockerfile
    container_name: chat
    environment:
      - NODE_ENV=production
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
    volumes:
      - chat_db:/app/database
    depends_on:
      rabbitmq:
        condition: service_healthy
      frontend:
        condition: service_started
      butler:
        condition: service_started
    networks:
      - transcendence
    restart: always

  profile:
    build:
      context: ./backend/database/
      dockerfile: profile/Dockerfile
    container_name: profile
    environment:
      - NODE_ENV=production
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
    volumes:
      - profile_db:/app/database
    depends_on:
      rabbitmq:
        condition: service_healthy
      frontend:
        condition: service_started
      butler:
        condition: service_started
    networks:
      - transcendence
    restart: always

  # GAME RELATED SERVICES
  ft_bunny:
    build:
      context: ./backend/services/ft_bunny
      dockerfile: Dockerfile
    container_name: ft_bunny
    depends_on:
      rabbitmq:
        condition: service_healthy
      butler:
        condition: service_started
    environment:
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
    networks:
      - transcendence
    restart: always

  lobby:
    build:
      context: ./backend/services/lobby
      dockerfile: Dockerfile
    container_name: lobby
    depends_on:
      ft_bunny:
        condition: service_started
      butler:
        condition: service_started
    networks:
      - transcendence
    restart: always

  tournament:
    build:
      context: ./backend/services/tournament
      dockerfile: Dockerfile
    container_name: tournament
    depends_on:
      ft_bunny:
        condition: service_started
      butler:
        condition: service_started
    networks:
      - transcendence
    restart: always

  pong:
    build:
      context: ./backend/services/pong
      dockerfile: Dockerfile
    container_name: pong
    depends_on:
      ft_bunny:
        condition: service_started
      butler:
        condition: service_started
    networks:
      - transcendence
    restart: always
  
  bots:
    build:
      context: ./backend/services/bots
      dockerfile: Dockerfile
    container_name: bots
    depends_on:
      ft_bunny:
        condition: service_started
      butler:
        condition: service_started
    networks:
      - transcendence
    restart: always

networks:
  transcendence:
    driver: bridge


volumes:
  auth_db:
  chat_db:
  profile_db:
  prometheus_dat:
  grafana_dat: